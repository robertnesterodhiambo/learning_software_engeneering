# Control flow 
this is how  instructions are executed
## verifying contion is met

   -if statements 
   ```dart
   void main (){
     bool football = true;
     if(football){
     print("go football);
     } else {
     print("go sports");
     }
     }
